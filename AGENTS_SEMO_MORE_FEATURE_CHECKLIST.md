<!-- Updated: 2026-03-24 -->

# Semo More Feature Checklist

이 문서는 `semo`에 새로운 `/more` 기능을 추가하거나 기존 기능을 대규모 재구성할 때 반드시 확인해야 하는 기준을 정리한 체크리스트입니다.

기본 전제:

- 모든 신규 기능은 `feature_catalog` 기준으로 관리합니다.
- 기능 활성화는 `feature_activation`으로 제어합니다.
- 유저 화면과 관리자 화면은 모두 `/more` 규칙을 따릅니다.
- 기능 전용 데이터는 기능 도메인 경계로 분리합니다.

## 1. 기능 정의

- 기능 한글명, 영문 key, 영문 경로 slug를 먼저 확정합니다.
- `feature_key`는 대문자 스네이크 또는 현재 카탈로그 규칙과 동일하게 유지합니다.
  - 예: `ATTENDANCE`, `NOTICE`, `ROLE_MANAGEMENT`
- 유저 경로와 관리자 경로를 둘 다 정의합니다.
  - 유저: `/clubs/[clubId]/more/<feature-slug>`
  - 관리자: `/clubs/[clubId]/admin/more/<feature-slug>`
- 기능 목적을 한 줄로 정의합니다.
  - “이 기능이 무엇을 해결하는지”
- 기능 소유 주체를 정의합니다.
  - 유저 사용 기능인지
  - 관리자 운영 기능인지
  - 둘 다 필요한지

## 2. 카탈로그 / 활성화

- `feature_catalog` seed를 추가합니다.
- `feature_activation` 조회/저장 대상에 포함되는지 확인합니다.
- `ClubFeatureService`의 user/admin path 매핑을 추가합니다.
- More 메뉴 응답에서 해당 기능이 정상 노출되는지 확인합니다.
- 기능이 꺼져 있을 때 유저/관리자 화면이 어떻게 동작해야 하는지 정합니다.
  - 메뉴 숨김
  - 접근 차단
  - 기존 데이터 보존 여부

## 3. 권한 모델

- 기본 권한 모델을 먼저 정의합니다.
  - `OWNER`, `ADMIN`은 전체 허용인지
  - `MEMBER`는 직책 권한을 통해 제어하는지
- `ROLE_MANAGEMENT` 활성화 여부에 따라 직책 권한 평가가 필요한지 정합니다.
- 권한 카탈로그를 `상위 기능`과 `하위 권한`으로 나눕니다.
  - 상위 기능: `feature_catalog`
  - 하위 권한: `feature_permission_catalog`
- 하위 권한은 실제 액션 단위로 정의합니다.
  - 예: `CREATE`, `UPDATE_SELF`, `DELETE_SELF`, `VIEW`, `ASSIGN`
- 소유권 범위가 필요한지 확인합니다.
  - `SELF`
  - `ANY`
- 기존 레거시 권한 테이블을 계속 읽는지, 완전히 대체하는지 명시합니다.

## 4. 직책 권한 연결

- 해당 기능의 하위 권한을 `feature_permission_catalog`에 seed 합니다.
- 직책관리 화면에 노출될 권한 그룹 이름과 설명을 정합니다.
- 활성화된 `more` 기능만 직책관리에서 보이게 할지 확인합니다.
  - 현재 `semo` 권장 규칙: 활성화된 기능만 노출
- 기능이 비활성화되었을 때 기존 직책 권한을:
  - 숨길지
  - 무시할지
  - 삭제할지
  를 결정합니다.
- `ClubPositionPermissionEvaluator`에서 필요한 permission key 상수를 추가합니다.
- 기능 서비스에서 실제 permission evaluator를 호출하는지 확인합니다.

## 5. DB 설계

- 기능 전용 테이블이 필요한지 판단합니다.
- 테이블은 기능 영문 타이틀 기준으로 시작합니다.
  - 예: `attendance_session`, `attendance_checkin`
  - 예: `notice_*`, `schedule_*`
- 테이블명은 `club_<feature>` 식 범용 네이밍보다 기능 도메인 중심으로 작성합니다.
- 단, 기존 도메인 확장이라 이미 네이밍 계열이 굳어져 있으면 그 패턴을 유지합니다.
  - 예: 기존 `club_notice`, `club_schedule_*` 계열은 그대로 유지
- 반드시 정해야 하는 항목:
  - 메인 엔터티 테이블
  - 하위 상태/이력 테이블
  - 연결 테이블
  - soft delete 여부
  - unique key
  - 대표 index
- 일/월/기간성 데이터면 기준 날짜 컬럼을 명확히 둡니다.
- 요약 화면이 필요하면 조회 비용을 고려해 인덱스를 먼저 설계합니다.

## 6. DDL / Seed / 운영 SQL

- 전체 DDL(`semo_ddl_all.sql`)에 반영합니다.
- 전체 seed(`semo_seed_all.sql`)에도 반영합니다.
- DB 구조가 바뀌면 먼저 기존 `all` 파일들을 최신 기준으로 수정합니다.
  - `semo_ddl_all.sql`
  - `semo_seed_all.sql`
- 운영 중인 DB 반영은 `all` 파일을 직접 실행하지 말고, 별도 운영 반영 SQL 파일을 따로 만듭니다.
- 운영 반영 SQL은 목적이 드러나는 이름으로 분리합니다.
  - 예: `semo_<feature>_apply.sql`
  - 예: `semo_<feature>_seed.sql`
  - 예: `semo_<feature>_cleanup.sql`
- seed는 신규 설치용 seed와 운영 반영 seed를 구분합니다.
- 운영 적용 순서를 문서화합니다.
  - apply
  - seed
  - cleanup
- 기존 운영 DB에 이미 데이터가 있으면 alter 전략과 롤백/백업 전략을 명시합니다.
- MySQL 호환 문법인지 확인합니다.
  - `IF NOT EXISTS` 등 버전 의존 문법 주의

## 7. 백엔드 구현

- 패키지는 `feature/<name>` 기준으로 만듭니다.
- 최소 구성:
  - `act/` controller
  - `biz/` service
  - `vo/` request/response DTO
- 서비스 메서드는 실제 도메인 규칙을 담고, 컨트롤러는 얇게 유지합니다.
- 트랜잭션 경계가 필요한 쓰기 작업은 명확히 둡니다.
- 기능 on/off 검사 로직을 서비스 진입부에 둡니다.
- 관리자/멤버 접근 제어를 명확히 분기합니다.
- 도메인 규칙이 “자동 생성”, “자동 종료”, “기간 만료” 같은 스케줄 성격이라면:
  - 진짜 스케줄러가 필요한지
  - lazy auto-create/auto-close 로 충분한지
  먼저 판단합니다.

## 8. 활동 로그

- 최근 활동에 남겨야 하는지 먼저 결정합니다.
- 활동 로그 대상이면 `RecordClubActivity`와 `ClubActivityContextHolder`를 적용합니다.
- 최소 기록 항목:
  - 활동 멤버: 행위자
  - 활동 주제: 기능명
  - 활동 내역: 사람이 읽는 문장
  - 성공/실패
- 실패 로그도 남겨야 하는지 판단합니다.
- 내부 동기화성 작업은 활동 로그에서 제외할지 검토합니다.

## 9. 유저 화면

- 유저 `/more` 화면 목적을 정의합니다.
  - 사용
  - 조회
  - 신청
  - 제출
- 신규 `more` 기능이면 유저 클럽 홈 대시보드 위젯도 함께 설계하는 것을 기본값으로 둡니다.
  - 예외는 “홈 위젯으로 요약할 가치가 전혀 없는 기능”일 때만 명시적으로 남깁니다.
- 유저 화면에서는 운영 도구보다 실제 사용 동작이 우선입니다.
- 유저 empty state를 정의합니다.
  - 기능 비활성
  - 데이터 없음
  - 참여 가능 시간 아님
- 유저 액션 성공/실패 toast와 버튼 문구를 정합니다.
- 유저 클럽 홈 위젯을 만들 때는 아래를 같이 정합니다.
  - 위젯 제목
  - 요약 지표
  - 클릭 시 이동 경로
  - 빈 상태 문구
  - 기능 비활성 시 숨김 여부

## 10. 관리자 화면

- 관리자 `/admin/more` 화면 목적을 정의합니다.
  - 운영 상태 요약
  - 생성/수정/종료
  - 멤버 관리
  - 통계
- 관리자 홈은 “운영 관점 데이터”를 우선합니다.
  - 전체 수
  - 진행 중 수
  - 최근 업데이트 시점
  - 오늘/이번 주/이번 달 요약
- 리스트가 꼭 필요한지, 요약만 필요한지 먼저 결정합니다.
- 직책관리처럼 카드 중심인지, 설정 페이지인지 UI 톤을 먼저 정합니다.
- 관리자용 CTA는 전역 버튼과 카드별 버튼이 중복되지 않게 설계합니다.

## 11. 색 테마 / UI 톤

- 유저 화면과 관리자 화면의 테마를 각각 정의합니다.
- 최소 정의 항목:
  - primary
  - accent
  - background
  - badge tone
  - empty state tone
- 유저는 기능 사용성 중심, 관리자는 운영/통제 중심으로 시각 톤을 나눕니다.
- 기존 `semo` 스타일과 충돌하지 않게 CSS 변수 또는 공통 토큰을 사용합니다.
- 유저 `/more` 신규 기능은 별도 독립 테마를 새로 만들기보다 기존 유저 페이지의 `primary`, `background`, 기본 카드 톤을 우선 재사용합니다.
- 기능 고유 색이 필요해도 보조 accent 수준으로만 쓰고, 유저 화면의 기본 버튼/배경/포커스 톤까지 갈아엎지 않습니다.
- 관리자 `/admin/more` 화면도 마찬가지로 기존 관리자 톤을 기본값으로 유지하고, 기능색은 보조적으로만 얹습니다.
- 같은 기능 내에서는 카드 UI, 토글 UI, 버튼 UI를 전부 통일합니다.

## 11-1. 공유 / 핀 / 라벨 정책

- `게시판에도 공유`, `캘린더에도 공유`는 단순 저장 플래그가 아니라 실제 노출 정책으로 봅니다.
- `postedToBoard = true`면 해당 항목은 게시판 피드에 실제로 노출되어야 합니다.
- `postedToCalendar = true`면 해당 항목은 캘린더 날짜 버킷에 실제로 노출되어야 합니다.
- 공유 대상 라벨은 노출되는 화면 안에서도 숨기지 않습니다.
  - 게시판에 공유된 항목은 게시판 화면에서도 `게시판` 배지를 보여줍니다.
  - 캘린더에 공유된 항목은 캘린더 화면에서도 `캘린더` 배지를 보여줍니다.
- 공유 라벨은 텍스트 설명이 아니라 배지로 통일합니다.
  - `게시판`
  - `캘린더`
- 고정(`pinned = true`)은 “게시판 중요 고정 게시물 영역 우선 노출” 의미로 사용합니다.
- 중요 고정 게시물 영역은 공지 전용으로 만들지 말고, 핀 가능한 도메인 전체를 고려합니다.
  - 현재 기준: 공지, 일정, 투표
- 중요 고정 게시물 영역과 일반 최근 게시글은 서로 다른 라벨 시스템을 만들지 않습니다.
- `more/notices`처럼 기능 홈이 따로 있는 화면에서는 “핀 전용 상단 섹션”이 꼭 필요한지 먼저 판단합니다.
  - 기능 홈 맥락이 강하면 핀 항목도 일반 목록에 함께 노출하는 편이 더 자연스럽습니다.
- 라벨 순서는 화면마다 바꾸지 말고 고정합니다.
  - 기본 순서: `타입` -> `고정/중요` -> `공유 대상`
- 타입 라벨 기본값:
  - `공지`
  - `일정`
  - `투표`
- 고정 라벨 기본값:
  - 카드/목록: `고정`
  - 게시판 중요 캐러셀: `중요`
- 투표 진행 상태는 사용자 참여 여부와 섞지 말고 lifecycle 기준으로만 표기합니다.
  - `마감`
  - `대기`
  - `진행 중`
- 투표 상태 라벨은 게시판, 캘린더, 투표 홈, 상세, 핀 캐러셀에서 같은 계산 로직을 사용합니다.
- 임시 해시 태그성 메타(`#응답 대기`, `#club_vote` 등)는 상태 라벨 체계에 포함하지 않습니다.
- 공유/핀/상태 라벨은 공통 유틸로 빼고, 화면별 하드코딩 복제를 만들지 않습니다.
  - 예: `content-badge.ts`, `vote-status.ts`

## 11-2. 카드 영역 / UIUX 정책

- 같은 도메인의 카드는 게시판, 기능 홈, 상세 진입 카드에서 문법을 가능한 한 통일합니다.
- 카드 상단 정보 구조는 아래 우선순위를 따릅니다.
  - 라벨 행
  - 날짜/시간/상태 메타
  - 제목
  - 요약
  - 작성자/행동 메뉴
- 배경형 카드(예: 중요 고정 공지 배경 이미지)도 라벨 규칙은 일반 카드와 동일해야 합니다.
- 배경 이미지 카드에서 가독성이 필요하면 이미지 투명도를 바꾸지 말고 오버레이 강도로 조절합니다.
- 이미지가 있는 일반 카드와 배경형 카드가 혼재해도 라벨 위치, 제목 위치, 하단 메타 구조는 최대한 유지합니다.
- `...` 메뉴는 카드 내부의 다른 버튼과 중첩되지 않게 만듭니다.
- 드롭다운 메뉴는 카드/목록의 `overflow`, `z-index` 구조에 막히지 않게 먼저 확인합니다.
- “중요 고정 게시물” 캐러셀과 일반 목록 카드가 공존하면 다음 항목을 반드시 비교합니다.
  - 배지 색
  - 배지 순서
  - 타이포 크기
  - 상단 여백
  - 본문-하단 경계선
  - 작성자 메타 구조
- 특정 화면만 예외 디자인이 필요하면, 왜 예외인지 문서에 이유를 남깁니다.

## 12. 프론트 연결

- `app/lib/clubs.ts`에 타입과 API 함수를 추가합니다.
- fallback client와 실제 client를 나눕니다.
- 라우트 파일에서 redirect 대신 실제 기능 client를 연결합니다.
- 기능 활성화 저장 후 필요한 이벤트가 있으면 전파합니다.
  - 예: `semo:club-features-updated`
- mock/preview 경로가 남아 있다면 영향 범위를 확인합니다.

## 13. 네이밍 규칙

- 기능 key: 카탈로그 기준 대문자 규칙 유지
- URL slug: kebab-case 또는 현재 more 구조와 일치
- 테이블명: 기능 영문 타이틀을 첫 단위로 시작
  - 예: `attendance_session`
  - 예: `attendance_checkin`
- 엔티티명/DTO명/서비스명도 기능 경계를 따릅니다.
  - 예: `ClubAttendanceService`
  - 예: `ClubAttendanceResponse`

## 14. 테스트 / 검증

- 백엔드
  - 최소 `:semo-back-service:compileJava`
  - 가능하면 해당 기능 서비스 테스트
  - 권한 분기 테스트
  - feature on/off 테스트
- 프론트
  - `npm run lint`
  - `npm run build`
- 기능이 권한과 직책관리에 연결되면:
  - OWNER/ADMIN 허용
  - MEMBER + ROLE_MANAGEMENT off
  - MEMBER + ROLE_MANAGEMENT on + 직책 권한 있음/없음
  시나리오를 확인합니다.

## 15. 문서 갱신

- 루트 `AGENTS.md`
- `AGENTS_DOCUMENTATION_INDEX.md`
- `semo-back-service/AGENTS.md`
- `semo-front-service/AGENTS.md`
- 필요하면 기능 전용 운영 SQL 문서

## 16. 신규 More 기능 최종 체크

- 기능 카탈로그 등록 완료
- feature activation 반영 완료
- user/admin path 매핑 완료
- 하위 권한 seed 완료
- 직책관리 노출 규칙 확인 완료
- DB 테이블/인덱스/DDL 완료
- 운영 DB 반영용 SQL 준비 완료
- 백엔드 API 구현 완료
- 활동 로그 연결 완료
- 유저 화면 완료
- 유저 홈 대시보드 위젯 완료 또는 예외 사유 문서화 완료
- 관리자 화면 완료
- 게시판 공유 정책 확인 완료
- 캘린더 공유 정책 확인 완료
- 핀 고정 정책 확인 완료
- 라벨 공통 유틸 적용 완료
- 중요 고정 영역과 일반 목록 배지 규칙 일치 확인 완료
- 색 테마 확정 완료
- lint/build/test 검증 완료
- 문서 갱신 완료
