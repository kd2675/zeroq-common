<!-- Updated: 2026-04-10 -->

# zeroq-common Sub-Agent Policy

## Critical Invariant

- 기본 구조는 `작업 지시자(depth 0) -> 중간관리자(depth 1) -> 서브에이전트(depth 2)`다.
- 이 구조는 `.codex` 설정과 `AGENTS.md`가 모두 현재 구조를 명시적으로 요구할 때만 유효하다.
- 활성 중간관리자는 항상 1개만 허용한다. 작업 플로우와 테스트 플로우는 중간관리자 간에 겹치면 안 된다.
- 파일 수정 권한은 작업 지시자와 중간관리자에게만 있다. 서브에이전트는 모두 advisory + read-only다.
- 동일 파일은 같은 시점에 한 역할만 소유할 수 있다.
- 이 규칙은 컨텍스트 압축 후에도 유지되어야 하는 **압축 불변 규칙**이다.

## Roles

### 작업 지시자

- 전체 목표를 분해하고 중간관리자를 순차적으로 할당한다.
- 범위, 수정 대상, 완료 조건, 검증 명령을 넘긴다.
- 중간관리자의 보고를 검토하고 다음 단계 시작 여부를 결정한다.
- 전역 통합과 최종 승인 책임을 가진다.

### 중간관리자

- 자신에게 할당된 범위의 실행 책임자다.
- 할당된 파일만 직접 수정한다.
- planner / researcher / coder / reviewer / fixer / tester를 필요 시 호출한다.
- 서브에이전트 출력을 검토하고 필요한 부분만 반영한다.
- 구현, 검토 대응, 검증, 보고를 끝까지 책임진다.

### 서브에이전트

- planner / researcher / coder / reviewer / fixer / tester만 사용한다.
- `worker`, `default`, 기타 임의 역할로 우회하지 않는다.
- 파일을 수정하지 않는다.
- 계획, 조사, 구현 초안, 리뷰, 패치 제안, 테스트 제안만 수행한다.
- 결과는 항상 중간관리자에게만 보고한다.

## Ownership Rules

- 작업 지시자는 활성 중간관리자에게 파일 소유권을 넘긴 뒤 같은 파일을 동시에 수정하지 않는다.
- 중간관리자는 범위 밖 수정이 필요하면 먼저 소유권 확장을 요청한다.
- 작업 지시자가 중간관리자 소유 파일을 직접 수정하려면 해당 중간관리자 작업을 종료하거나 명시적으로 인터럽트해야 한다.

## Standard Flow

### 1. Assign

작업 지시자는 중간관리자에게 아래를 전달한다.

- 사용자 의도
- 현재 방향
- 작업 경계
- 예상 수정 파일 또는 모듈
- 완료 조건
- 필수 검증 명령

### 2. Plan

- 소규모 작업이면 중간관리자가 직접 계획한다.
- 중규모 이상이거나 트레이드오프가 있으면 planner를 먼저 호출한다.

### 3. Research

다음 경우 researcher를 먼저 호출한다.

- 프레임워크 또는 라이브러리 문법이 버전에 따라 달라질 수 있을 때
- deprecation, migration, breaking change 가능성이 있을 때
- 외부 API / SDK / 공식 문서 확인이 필요할 때
- 현재 코드베이스만으로는 에러나 경고 해석이 불충분할 때

researcher는 항상 실제 의존성 버전을 먼저 확인하고, 그 버전에 맞는 공식 문법과 기존 코드 패턴을 함께 정리한다.

### 4. Implement

- 중간관리자가 직접 구현한다.
- 초안 비교가 필요할 때만 coder를 호출한다.
- 버그 수정의 첫 패치는 기본적으로 fixer를 우선 사용한다.

### 5. Review

- 완료 후보 상태가 되면 reviewer를 호출한다.
- FAIL이면 fixer 또는 중간관리자가 수정하고 reviewer 재검토를 받는다.
- 리뷰 루프는 항목당 최대 2회다.
- 2회 초과 시 중간관리자가 직접 판단하고, 사유를 작업 지시자에게 보고한다.

### 6. Verify

- tester를 호출해 테스트 전략 또는 실패 분석을 받는다.
- 중간관리자는 필요한 테스트를 작성/수정하고 실제 명령을 실행한다.
- FAIL이면 fixer 또는 중간관리자가 수정하고 tester 재검토를 받는다.
- 검증 루프는 최대 2회다.
- 2회 초과 시 중간관리자가 직접 판단하고, 사유를 작업 지시자에게 보고한다.

### 7. Report

중간관리자는 아래 형식으로 작업 지시자에게 보고한다.

```text
STATUS: done | blocked | unverified
SCOPE: owned scope
CHANGED FILES: path list
VERIFICATION: commands + result
RISKS: remaining risk or assumption
NEXT HANDOFF: next decision or next work package
```

작업 지시자는 이 보고를 받은 뒤에만 다음 중간관리자를 시작한다.

## Mandatory Rules

- reviewer 또는 동등한 코드 검토 없이 완료 처리하지 않는다.
- tester 기반 검증 또는 동등한 실제 실행 검증 없이 완료 처리하지 않는다.
- "변경이 작다", "명백하다", "시간이 오래 걸린다" 같은 이유로 리뷰나 검증을 생략하지 않는다.
- 검증을 실행하지 못했다면 완료가 아니라 `unverified`로 보고한다.
- 예외를 사용했다면 작업 지시자에게 명시적으로 보고한다.

## Short Flows

### 버그 수정

`중간관리자 -> researcher(필요 시) -> fixer -> reviewer -> tester -> 작업 지시자 보고`

- 버그 수정에서도 reviewer / tester를 생략하지 않는다.
- 재현 실패 시에도 reviewer 검토는 유지한다.

### 리뷰 전용

`작업 지시자 범위 지정 -> 중간관리자 정리 -> reviewer -> 중간관리자 종합 -> 작업 지시자 보고`

## Agent Use Guide

- planner: 계획이나 분해가 모호할 때
- researcher: 버전 민감 문법, 외부 문서, 원인 조사 필요 시
- coder: 구현 초안 비교가 필요할 때만
- fixer: 버그 수정 또는 리뷰/테스트 FAIL 대응
- reviewer: 완료 후보 검토
- tester: 테스트 전략 수립과 실패 분석

중간관리자는 서브에이전트 제안을 그대로 적용하지 말고, 의도, 버전, 범위, 검증 계획 기준으로 선별 반영한다.
